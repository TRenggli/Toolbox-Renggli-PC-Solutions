# Normalizacion de finales de linea para los scripts.
# Los scripts de shell corren en Linux/macOS y DEBEN usar LF
# (un \r de CRLF rompe `bash -n` y la ejecucion en Linux; era la causa del CI en rojo).
*.sh text eol=lf

# Los scripts de Windows usan CRLF.
*.bat text eol=crlf
*.cmd text eol=crlf
